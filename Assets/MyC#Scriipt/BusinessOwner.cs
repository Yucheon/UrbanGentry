using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BusinessOwner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int BO_Coin =7;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
